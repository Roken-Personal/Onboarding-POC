{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && rm -rf node_modules/.prisma node_modules/@prisma/client && PRISMA_QUERY_ENGINE_LIBRARY=debian-openssl-3.0.x npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}

